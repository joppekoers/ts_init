{
	"name": "ts_init",
	"version": "",
	"description": "",
	"scripts": {
		"dev": "bun run --watch src/app.ts",
		"start": "bun run src/app.ts",
		"build": "tsc",
		"typecheck": "tsc --noEmit",
		"lint:check": "eslint --cache --cache-location node_modules/.eslintcache . && prettier --check .",
		"lint:fix": "eslint --cache --cache-location node_modules/.eslintcache --fix . && prettier --write .",
		"test": "bun test"
	},
	"dependencies": {},
	"devDependencies": {
		"@eslint/js": "^10.0.0",
		"@types/bun": "latest",
		"eslint": "^10.0.0",
		"eslint-config-prettier": "^10.0.0",
		"prettier": "^3.0.0",
		"typescript": "^6.0.0",
		"typescript-eslint": "^8.58.0"
	}
}
